# Browser-safe Vite configuration
VITE_SUPABASE_URL=https://csolgywkgummefnwouny.supabase.co
VITE_SUPABASE_ANON_KEY=your_publishable_or_legacy_anon_key

# Never add provider credentials with a VITE_ prefix. Vite variables are public.
# Configure these as Supabase Edge Function secrets instead:
# GEMINI_API_KEY
# NVIDIA_API_KEY
# BFL_API_KEY
# OPENAI_API_KEY (future adapter; currently disabled)

# Server-owned generation defaults and spending policy are stored in the
# database/Edge Function environment, not in browser localStorage.
